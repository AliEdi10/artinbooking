import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Reset Password | Artin Driving School',
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
    return children;
}
