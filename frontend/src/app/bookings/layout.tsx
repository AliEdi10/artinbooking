import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Bookings | Artin Driving School',
};

export default function BookingsLayout({ children }: { children: React.ReactNode }) {
    return children;
}
