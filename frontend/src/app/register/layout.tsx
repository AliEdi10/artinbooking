import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Register | Artin Driving School',
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
    return children;
}
