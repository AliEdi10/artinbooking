# Backend Environment Variables
# Copy this file to .env and fill in the values, or set via start-dev.ps1

# PostgreSQL Database Connection
PGHOST=localhost
PGPORT=5434
PGUSER=artinbk
PGPASSWORD=localdevpw
PGDATABASE=artinbk

# Server Port
PORT=3001

# Node Environment (development | production)
NODE_ENV=development

# JWT Configuration
# For development, uses local RS256 key pair in keys/ directory
# For production, set JWT_PUBLIC_KEY and JWT_PRIVATE_KEY as base64 strings
# JWT_PUBLIC_KEY=
# JWT_PRIVATE_KEY=

# Email (Resend)
# Required for sending invitation, booking, and password-reset emails
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev

# Frontend URL (used in email links)
FRONTEND_URL=http://localhost:3000

# App display name (used in email subjects/body)
APP_NAME=Artin Driving School
